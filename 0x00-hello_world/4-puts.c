#include<stdio.h>
#include <string.h>
/**
 * main-Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char name[60] = "\"Programming is like building a multilingual puzzle";

	puts(name);
	return (0);
}
